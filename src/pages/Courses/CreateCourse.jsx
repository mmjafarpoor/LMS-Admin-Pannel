import { Card, CardHeader, CardBody, CardTitle, CardText } from "reactstrap";
import FormWizard from "../../components/CoursesManagement/CreateCourse";

const CreateCourse = () => {

  return (
    <FormWizard/>
  );
};

export default CreateCourse;
